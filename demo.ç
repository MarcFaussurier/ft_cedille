#import "test.c"

macro int f(int x)
{
	return (x);
}
macro rule
(
	<s[i + x] == "Hello"[x] 	; x >= 5>  
		<name1[x] = c[i + x] 	; c[i + x] != '!'>
)
{
	return(cat("Hello ", name1));
}

int main(int ac, char **av)
{
	printf ("Hello aa, bb!");
	return (0);
}
