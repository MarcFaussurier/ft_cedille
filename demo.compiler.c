#include "get_next_linev2/get_next_line.h"
#include "stdio.h"
#include "string.h"

fns
 	...

int main(int ac, char **av)
{
	if (ac == 1)
		return printf ("Usage: %s <file1.ç.c> <file2.ç.c> ...\n", av[0]);
	if (ac < 1)
		return printf ("Usage: ./compiler <file1.ç.c> <file2.ç.c>...\n", av[0]);
	while (--ac)
	while (get token..)
	{
		if (match)
			fns ....
	}
}
